# Bandit Level 10 → 11

## Objective

Decode the password stored in `data.txt` which contains base64 encoded data.

## Skills Learned

- Base64 Decoding

## Commands Used

base64 -d data.txt

## What I Learned

- เข้าใจหลักการทำงานของรูปแบบโครงสร้างการเข้ารหัส Base64 และการเรียกใช้ชุดคำสั่ง `base64 -d` เพื่อทำหน้าที่ถอดรหัสคืนรูปเดิม